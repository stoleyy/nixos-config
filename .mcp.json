{
  "mcpServers": {
    "nixos": {
      "command": "uvx",
      "args": ["mcp-nixos"],
      "description": "Live access to search.nixos.org so Claude validates option paths against the actual nixpkgs release instead of pattern-matching from training data. Requires `uvx` (install via `pipx install uv` or `nix run nixpkgs#uv`)."
    }
  }
}
