# This file must be encrypted with sops before use.
#
# Setup (run once on the predator machine):
#   1. Get the age pubkey from the host SSH key:
#        nix-shell -p ssh-to-age --run "ssh-to-age < /etc/ssh/ssh_host_ed25519_key.pub"
#   2. Replace the placeholder in ../.sops.yaml with the output above.
#   3. Encrypt and populate this file:
#        nix-shell -p sops --run "sops secrets/secrets.yaml"
#
# After populating, commit the encrypted file (it is safe to commit).
placeholder: "replace-me"
